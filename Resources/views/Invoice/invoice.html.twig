<div class="panel panel-body">
    <div class="top default-block top-margin">
        <div class="div-buttons pull-right">
            <a href="{{ path('invoice_show_all') }}" class="btn btn-primary pull-right"> {{ 'my_invoices'|trans({}, 'invoice') }} </a>
            </br></br>
            <a href="{{ path('invoice_download', {'invoice': chart.getInvoice().getId()}) }}" class="btn btn-default btn pull-right"> {{ 'download'|trans({}, 'platform') }} </a>
        </div>
    </div>
    {% include 'FormaLibreInvoiceBundle:Invoice:invoice\\invoice_data.html.twig'
        with {'chart': chart}
    %}
    {% include 'FormaLibreInvoiceBundle:Invoice:invoice\\chart_data.html.twig'
        with {'chart': chart}
    %}
    {% include 'FormaLibreInvoiceBundle:Invoice:invoice\\footer.html.twig'
        with {'communication': communication, 'chart': chart}
    %}
</div>
