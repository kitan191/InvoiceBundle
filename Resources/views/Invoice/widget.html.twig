</div class="panel panel-default">
    <div class="panel-body">
        <table class="table table-striped table-bordered table-condensed">
            <thead>
                <tr>
                    <th> {{ 'id'|trans({}, 'platform') }} </th>
                    <th> </th>
                </tr>
            </thead>
            <tbody>
                {% for chart in charts %}
                    <tr>
                        <th> {{ chart.getInvoice().getInvoiceNumber()}} </th>
                        <th> <a href="{{ path('admin_invoice_show', {'invoice': chart.getInvoice().getId()} ) }}"> {{ 'display'|trans({}, 'platform') }} </a> </th>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
