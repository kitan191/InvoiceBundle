<!DOCTYPE html>

{% set communication = null %}

<html>
    <head>
      <title> formalibre </title>
      <link rel="stylesheet" href="{{ asset('', absolute=true) ~ 'themes/' ~ 'claroline' ~ '/bootstrap.css' }}" screen="media" />
      <style>
          .top-margin {
              margin-top: 10%;
          }
          .default-block {
              margin-left:10%;
          }
         .logo {
                max-width:20%;
                height: auto;
          }
      </style>
    </head>
    <body>
        <img src="{{ asset('bundles/formalibreinvoice/images/invoice_logo.jpg', absolute=true) }}" class="logo" alt="logo"/></img>
        <div class="panel panel-default">
            <div class="panel panel-body">
                {% include 'FormaLibreInvoiceBundle:Invoice:invoice\\invoice_data.html.twig'
                    with {'chart': chart}
                %}
                {% include 'FormaLibreInvoiceBundle:Invoice:invoice\\chart_data.html.twig'
                    with {'chart': chart}
                %}
                {% include 'FormaLibreInvoiceBundle:Invoice:invoice\\bank_transfer.html.twig'
                    with {'communication': communication, 'chart': chart}
                %}
            </div>
        </div>
    </body>
</html>
