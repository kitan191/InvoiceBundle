<!DOCTYPE html>

<html>
    <head>
      <title> formalibre </title>
      <style>
          ul, li {
            list-style-type: none;
            list-style-position:inside;
            margin:0;
            padding:0;
          }

          .top-margin {
              margin-top: 10%;
          }

          .footer {
              text-align:center;
          }

          .default-block {
              margin-left:10%;
          }

          .client-info {
              margin-left:60%;
          }

          .logo {
                max-width:20%;
                height: auto;
          }

          .top-span {
              display: inline-block;
          }
      </style>
    </head>

    <body>
        <div class="top default-block top-margin">
            <img src="{{ asset('bundles/formalibreinvoice/images/invoice_logo.jpg', absolute=true) }}" class="logo" alt="logo_formalibre"/>
        </div>
        <div class="top-margin title">
            Merci de votre achat Forma-Libre
        </div>
        <div class="top-margin credit">
            Monsieur / Madame {{ order.getOwner().getLastName() }}, nous avons bien reçu votre virement. Votre espace d'activité ({{ workspace.code }}) est a présent disponible.
        </div>
        <div class="payment-type default-block top-margin">
            Numéro de commande: {{ order.getId() }}
            <ul style="list-style-type: none;">
                <li> {{ order.getOwner().getLastName() }} {{ order.getOwner().getFirstName() }} </li>
                {% if company is defined %} <li> {{ company}} </li> {% endif %}
                <li> Date de commande: {{ order.getCreationDate().getTimeStamp()|date('d/m/Y') }} </li>
                <li> Mode de paiment: {{ order.getPaymentInstruction().getPaymentSystemName()|trans({}, 'invoice') }} </li>
            </ul>
        </div>
        <div class="item-description default-block top-margin">
            Description du produit:
            <ul style="list-style-type: none;">
                <li>Nom: {{ order.getProduct().getCode()|trans({}, 'invoice') }}</li>
                <li>Nombre d'utilisateur: {{ order.product.details.max_users }}</li>
                <li>Nombre de ressource: {{ order.product.details.max_resources }}</li>
                <li>Espace de stockage: {{ order.product.details.max_storage }} </li>
                <li>Durée: {{ month_duration }} mois </li>
                <li>Date d'expiration: {{ sws.getExpDate()|date('d/m/Y') }} </li>
            </ul>
        </div>
        <div class="url-container default-block top-margin">
            Entrez dans votre espace d'activité à l'adresse suivante: {{ target_adress }}
        </div>
        {% if hasFreeMonth %} 1 mois gratuit dans le tas {% endif %}
        <div id="footer" class="footer top-margin">
            <small>
                {{ 'invoice_footer'|trans({}, 'invoice')|raw }}
            </small>
        </div>
    </body>

</html>
